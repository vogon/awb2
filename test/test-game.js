var Game = require('../game.js');

// verify that a game is empty upon creation
// verify that a game can't be started with fewer than 3 players
// verify that a game can't be restarted after it's been started

